"use client";

export default function DataPage() {
  return (
    <div className="min-h-screen bg-slate-50 px-6 py-8">
      <div className="mx-auto max-w-3xl rounded-2xl border border-slate-200 bg-white p-6">
        <h1 className="text-2xl font-bold text-slate-900">Data Page Disabled (Demo)</h1>
        <p className="mt-2 text-sm text-slate-600">
          This page is intentionally commented out for the demo. Use the Rules page instead.
        </p>

        {/* Demo mode: data viewer implementation is commented out. */}
        {/* Original CRM DataTable page is intentionally hidden, not deleted. */}
      </div>
    </div>
  );
}
